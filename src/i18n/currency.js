// Moneda por omision de formatCurrency cuando la pantalla no pasa una. Cambiala por la de tu producto
// (o resuelvela desde un claim del JWT, como hace un producto multi-moneda). Vive en su propio modulo
// para que I18nProvider.jsx solo exporte componentes (react-refresh).
export const DEFAULT_CURRENCY = 'USD'
